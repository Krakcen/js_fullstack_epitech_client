import { Segment } from 'semantic-ui-react';
import styled from 'styled-components';

const StoryFinishedSegment = styled(Segment)`
  &&&&& {
    background-color: transparent;
    border-radius: 20px;
    box-shadow: none;
    border: 3px solid white;
    padding: 12px;
  }
`;

export default StoryFinishedSegment;
